// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package es.uca.iw.tempojobs.domain;

import es.uca.iw.tempojobs.domain.UsuarioEmpresa;
import es.uca.iw.tempojobs.domain.UsuarioEmpresaDataOnDemand;
import es.uca.iw.tempojobs.domain.UsuarioEmpresaIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect UsuarioEmpresaIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UsuarioEmpresaIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UsuarioEmpresaIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UsuarioEmpresaIntegrationTest: @Transactional;
    
    @Autowired
    UsuarioEmpresaDataOnDemand UsuarioEmpresaIntegrationTest.dod;
    
    @Test
    public void UsuarioEmpresaIntegrationTest.testCountUsuarioEmpresas() {
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to initialize correctly", dod.getRandomUsuarioEmpresa());
        long count = UsuarioEmpresa.countUsuarioEmpresas();
        Assert.assertTrue("Counter for 'UsuarioEmpresa' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UsuarioEmpresaIntegrationTest.testFindUsuarioEmpresa() {
        UsuarioEmpresa obj = dod.getRandomUsuarioEmpresa();
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to provide an identifier", id);
        obj = UsuarioEmpresa.findUsuarioEmpresa(id);
        Assert.assertNotNull("Find method for 'UsuarioEmpresa' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UsuarioEmpresa' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UsuarioEmpresaIntegrationTest.testFindAllUsuarioEmpresas() {
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to initialize correctly", dod.getRandomUsuarioEmpresa());
        long count = UsuarioEmpresa.countUsuarioEmpresas();
        Assert.assertTrue("Too expensive to perform a find all test for 'UsuarioEmpresa', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UsuarioEmpresa> result = UsuarioEmpresa.findAllUsuarioEmpresas();
        Assert.assertNotNull("Find all method for 'UsuarioEmpresa' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UsuarioEmpresa' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UsuarioEmpresaIntegrationTest.testFindUsuarioEmpresaEntries() {
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to initialize correctly", dod.getRandomUsuarioEmpresa());
        long count = UsuarioEmpresa.countUsuarioEmpresas();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UsuarioEmpresa> result = UsuarioEmpresa.findUsuarioEmpresaEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UsuarioEmpresa' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UsuarioEmpresa' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UsuarioEmpresaIntegrationTest.testFlush() {
        UsuarioEmpresa obj = dod.getRandomUsuarioEmpresa();
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to provide an identifier", id);
        obj = UsuarioEmpresa.findUsuarioEmpresa(id);
        Assert.assertNotNull("Find method for 'UsuarioEmpresa' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyUsuarioEmpresa(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'UsuarioEmpresa' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void UsuarioEmpresaIntegrationTest.testMergeUpdate() {
        UsuarioEmpresa obj = dod.getRandomUsuarioEmpresa();
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to provide an identifier", id);
        obj = UsuarioEmpresa.findUsuarioEmpresa(id);
        boolean modified =  dod.modifyUsuarioEmpresa(obj);
        Integer currentVersion = obj.getVersion();
        UsuarioEmpresa merged = (UsuarioEmpresa)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'UsuarioEmpresa' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void UsuarioEmpresaIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to initialize correctly", dod.getRandomUsuarioEmpresa());
        UsuarioEmpresa obj = dod.getNewTransientUsuarioEmpresa(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UsuarioEmpresa' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'UsuarioEmpresa' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UsuarioEmpresaIntegrationTest.testRemove() {
        UsuarioEmpresa obj = dod.getRandomUsuarioEmpresa();
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UsuarioEmpresa' failed to provide an identifier", id);
        obj = UsuarioEmpresa.findUsuarioEmpresa(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UsuarioEmpresa' with identifier '" + id + "'", UsuarioEmpresa.findUsuarioEmpresa(id));
    }
    
}
