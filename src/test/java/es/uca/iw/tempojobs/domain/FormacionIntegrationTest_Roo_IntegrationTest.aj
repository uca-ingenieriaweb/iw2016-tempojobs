// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package es.uca.iw.tempojobs.domain;

import es.uca.iw.tempojobs.domain.Formacion;
import es.uca.iw.tempojobs.domain.FormacionDataOnDemand;
import es.uca.iw.tempojobs.domain.FormacionIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect FormacionIntegrationTest_Roo_IntegrationTest {
    
    declare @type: FormacionIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: FormacionIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: FormacionIntegrationTest: @Transactional;
    
    @Autowired
    FormacionDataOnDemand FormacionIntegrationTest.dod;
    
    @Test
    public void FormacionIntegrationTest.testCountFormacions() {
        Assert.assertNotNull("Data on demand for 'Formacion' failed to initialize correctly", dod.getRandomFormacion());
        long count = Formacion.countFormacions();
        Assert.assertTrue("Counter for 'Formacion' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void FormacionIntegrationTest.testFindFormacion() {
        Formacion obj = dod.getRandomFormacion();
        Assert.assertNotNull("Data on demand for 'Formacion' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Formacion' failed to provide an identifier", id);
        obj = Formacion.findFormacion(id);
        Assert.assertNotNull("Find method for 'Formacion' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Formacion' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void FormacionIntegrationTest.testFindAllFormacions() {
        Assert.assertNotNull("Data on demand for 'Formacion' failed to initialize correctly", dod.getRandomFormacion());
        long count = Formacion.countFormacions();
        Assert.assertTrue("Too expensive to perform a find all test for 'Formacion', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Formacion> result = Formacion.findAllFormacions();
        Assert.assertNotNull("Find all method for 'Formacion' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Formacion' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void FormacionIntegrationTest.testFindFormacionEntries() {
        Assert.assertNotNull("Data on demand for 'Formacion' failed to initialize correctly", dod.getRandomFormacion());
        long count = Formacion.countFormacions();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Formacion> result = Formacion.findFormacionEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Formacion' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Formacion' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void FormacionIntegrationTest.testFlush() {
        Formacion obj = dod.getRandomFormacion();
        Assert.assertNotNull("Data on demand for 'Formacion' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Formacion' failed to provide an identifier", id);
        obj = Formacion.findFormacion(id);
        Assert.assertNotNull("Find method for 'Formacion' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyFormacion(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Formacion' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void FormacionIntegrationTest.testMergeUpdate() {
        Formacion obj = dod.getRandomFormacion();
        Assert.assertNotNull("Data on demand for 'Formacion' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Formacion' failed to provide an identifier", id);
        obj = Formacion.findFormacion(id);
        boolean modified =  dod.modifyFormacion(obj);
        Integer currentVersion = obj.getVersion();
        Formacion merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Formacion' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void FormacionIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Formacion' failed to initialize correctly", dod.getRandomFormacion());
        Formacion obj = dod.getNewTransientFormacion(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Formacion' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Formacion' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'Formacion' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void FormacionIntegrationTest.testRemove() {
        Formacion obj = dod.getRandomFormacion();
        Assert.assertNotNull("Data on demand for 'Formacion' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Formacion' failed to provide an identifier", id);
        obj = Formacion.findFormacion(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Formacion' with identifier '" + id + "'", Formacion.findFormacion(id));
    }
    
}
