// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package es.uca.iw.tempojobs.domain;

import es.uca.iw.tempojobs.domain.Puesto;
import es.uca.iw.tempojobs.domain.PuestoDataOnDemand;
import es.uca.iw.tempojobs.domain.PuestoIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PuestoIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PuestoIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PuestoIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PuestoIntegrationTest: @Transactional;
    
    @Autowired
    PuestoDataOnDemand PuestoIntegrationTest.dod;
    
    @Test
    public void PuestoIntegrationTest.testCountPuestoes() {
        Assert.assertNotNull("Data on demand for 'Puesto' failed to initialize correctly", dod.getRandomPuesto());
        long count = Puesto.countPuestoes();
        Assert.assertTrue("Counter for 'Puesto' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PuestoIntegrationTest.testFindPuesto() {
        Puesto obj = dod.getRandomPuesto();
        Assert.assertNotNull("Data on demand for 'Puesto' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Puesto' failed to provide an identifier", id);
        obj = Puesto.findPuesto(id);
        Assert.assertNotNull("Find method for 'Puesto' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Puesto' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PuestoIntegrationTest.testFindAllPuestoes() {
        Assert.assertNotNull("Data on demand for 'Puesto' failed to initialize correctly", dod.getRandomPuesto());
        long count = Puesto.countPuestoes();
        Assert.assertTrue("Too expensive to perform a find all test for 'Puesto', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Puesto> result = Puesto.findAllPuestoes();
        Assert.assertNotNull("Find all method for 'Puesto' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Puesto' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PuestoIntegrationTest.testFindPuestoEntries() {
        Assert.assertNotNull("Data on demand for 'Puesto' failed to initialize correctly", dod.getRandomPuesto());
        long count = Puesto.countPuestoes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Puesto> result = Puesto.findPuestoEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Puesto' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Puesto' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PuestoIntegrationTest.testFlush() {
        Puesto obj = dod.getRandomPuesto();
        Assert.assertNotNull("Data on demand for 'Puesto' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Puesto' failed to provide an identifier", id);
        obj = Puesto.findPuesto(id);
        Assert.assertNotNull("Find method for 'Puesto' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyPuesto(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Puesto' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PuestoIntegrationTest.testMergeUpdate() {
        Puesto obj = dod.getRandomPuesto();
        Assert.assertNotNull("Data on demand for 'Puesto' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Puesto' failed to provide an identifier", id);
        obj = Puesto.findPuesto(id);
        boolean modified =  dod.modifyPuesto(obj);
        Integer currentVersion = obj.getVersion();
        Puesto merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Puesto' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PuestoIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Puesto' failed to initialize correctly", dod.getRandomPuesto());
        Puesto obj = dod.getNewTransientPuesto(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Puesto' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Puesto' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'Puesto' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PuestoIntegrationTest.testRemove() {
        Puesto obj = dod.getRandomPuesto();
        Assert.assertNotNull("Data on demand for 'Puesto' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Puesto' failed to provide an identifier", id);
        obj = Puesto.findPuesto(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Puesto' with identifier '" + id + "'", Puesto.findPuesto(id));
    }
    
}
