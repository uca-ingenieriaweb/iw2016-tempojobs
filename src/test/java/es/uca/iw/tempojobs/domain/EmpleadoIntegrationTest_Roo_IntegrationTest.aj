// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package es.uca.iw.tempojobs.domain;

import es.uca.iw.tempojobs.domain.Empleado;
import es.uca.iw.tempojobs.domain.EmpleadoDataOnDemand;
import es.uca.iw.tempojobs.domain.EmpleadoIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect EmpleadoIntegrationTest_Roo_IntegrationTest {
    
    declare @type: EmpleadoIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: EmpleadoIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: EmpleadoIntegrationTest: @Transactional;
    
    @Autowired
    EmpleadoDataOnDemand EmpleadoIntegrationTest.dod;
    
    @Test
    public void EmpleadoIntegrationTest.testCountEmpleadoes() {
        Assert.assertNotNull("Data on demand for 'Empleado' failed to initialize correctly", dod.getRandomEmpleado());
        long count = Empleado.countEmpleadoes();
        Assert.assertTrue("Counter for 'Empleado' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void EmpleadoIntegrationTest.testFindEmpleado() {
        Empleado obj = dod.getRandomEmpleado();
        Assert.assertNotNull("Data on demand for 'Empleado' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Empleado' failed to provide an identifier", id);
        obj = Empleado.findEmpleado(id);
        Assert.assertNotNull("Find method for 'Empleado' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Empleado' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void EmpleadoIntegrationTest.testFindAllEmpleadoes() {
        Assert.assertNotNull("Data on demand for 'Empleado' failed to initialize correctly", dod.getRandomEmpleado());
        long count = Empleado.countEmpleadoes();
        Assert.assertTrue("Too expensive to perform a find all test for 'Empleado', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Empleado> result = Empleado.findAllEmpleadoes();
        Assert.assertNotNull("Find all method for 'Empleado' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Empleado' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void EmpleadoIntegrationTest.testFindEmpleadoEntries() {
        Assert.assertNotNull("Data on demand for 'Empleado' failed to initialize correctly", dod.getRandomEmpleado());
        long count = Empleado.countEmpleadoes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Empleado> result = Empleado.findEmpleadoEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Empleado' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Empleado' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void EmpleadoIntegrationTest.testFlush() {
        Empleado obj = dod.getRandomEmpleado();
        Assert.assertNotNull("Data on demand for 'Empleado' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Empleado' failed to provide an identifier", id);
        obj = Empleado.findEmpleado(id);
        Assert.assertNotNull("Find method for 'Empleado' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyEmpleado(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Empleado' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void EmpleadoIntegrationTest.testMergeUpdate() {
        Empleado obj = dod.getRandomEmpleado();
        Assert.assertNotNull("Data on demand for 'Empleado' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Empleado' failed to provide an identifier", id);
        obj = Empleado.findEmpleado(id);
        boolean modified =  dod.modifyEmpleado(obj);
        Integer currentVersion = obj.getVersion();
        Empleado merged = (Empleado)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Empleado' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void EmpleadoIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Empleado' failed to initialize correctly", dod.getRandomEmpleado());
        Empleado obj = dod.getNewTransientEmpleado(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Empleado' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Empleado' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'Empleado' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void EmpleadoIntegrationTest.testRemove() {
        Empleado obj = dod.getRandomEmpleado();
        Assert.assertNotNull("Data on demand for 'Empleado' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Empleado' failed to provide an identifier", id);
        obj = Empleado.findEmpleado(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Empleado' with identifier '" + id + "'", Empleado.findEmpleado(id));
    }
    
}
