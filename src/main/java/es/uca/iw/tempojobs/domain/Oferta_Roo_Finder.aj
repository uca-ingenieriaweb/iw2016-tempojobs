// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package es.uca.iw.tempojobs.domain;

import es.uca.iw.tempojobs.domain.Oferta;
import javax.persistence.EntityManager;
import javax.persistence.TypedQuery;

privileged aspect Oferta_Roo_Finder {
    
    public static Long Oferta.countFindOfertasByFormacionLike(String formacion) {
        if (formacion == null || formacion.length() == 0) throw new IllegalArgumentException("The formacion argument is required");
        formacion = formacion.replace('*', '%');
        if (formacion.charAt(0) != '%') {
            formacion = "%" + formacion;
        }
        if (formacion.charAt(formacion.length() - 1) != '%') {
            formacion = formacion + "%";
        }
        EntityManager em = Oferta.entityManager();
        TypedQuery q = em.createQuery("SELECT COUNT(o) FROM Oferta AS o WHERE LOWER(o.formacion) LIKE LOWER(:formacion)", Long.class);
        q.setParameter("formacion", formacion);
        return ((Long) q.getSingleResult());
    }
    
    public static TypedQuery<Oferta> Oferta.findOfertasByFormacionLike(String formacion) {
        if (formacion == null || formacion.length() == 0) throw new IllegalArgumentException("The formacion argument is required");
        formacion = formacion.replace('*', '%');
        if (formacion.charAt(0) != '%') {
            formacion = "%" + formacion;
        }
        if (formacion.charAt(formacion.length() - 1) != '%') {
            formacion = formacion + "%";
        }
        EntityManager em = Oferta.entityManager();
        TypedQuery<Oferta> q = em.createQuery("SELECT o FROM Oferta AS o WHERE LOWER(o.formacion) LIKE LOWER(:formacion)", Oferta.class);
        q.setParameter("formacion", formacion);
        return q;
    }
    
    public static TypedQuery<Oferta> Oferta.findOfertasByFormacionLike(String formacion, String sortFieldName, String sortOrder) {
        if (formacion == null || formacion.length() == 0) throw new IllegalArgumentException("The formacion argument is required");
        formacion = formacion.replace('*', '%');
        if (formacion.charAt(0) != '%') {
            formacion = "%" + formacion;
        }
        if (formacion.charAt(formacion.length() - 1) != '%') {
            formacion = formacion + "%";
        }
        EntityManager em = Oferta.entityManager();
        StringBuilder queryBuilder = new StringBuilder("SELECT o FROM Oferta AS o WHERE LOWER(o.formacion) LIKE LOWER(:formacion)");
        if (fieldNames4OrderClauseFilter.contains(sortFieldName)) {
            queryBuilder.append(" ORDER BY ").append(sortFieldName);
            if ("ASC".equalsIgnoreCase(sortOrder) || "DESC".equalsIgnoreCase(sortOrder)) {
                queryBuilder.append(" ").append(sortOrder);
            }
        }
        TypedQuery<Oferta> q = em.createQuery(queryBuilder.toString(), Oferta.class);
        q.setParameter("formacion", formacion);
        return q;
    }
    
}
