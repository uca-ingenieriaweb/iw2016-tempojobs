<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security" 
    xmlns:beans="http://www.springframework.org/schema/beans" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd">
    <!-- HTTP security configurations -->
    <http auto-config="true" use-expressions="true">
        <form-login login-processing-url="/resources/j_spring_security_check" login-page="/login" authentication-failure-url="/login?login_error=t" />
		<logout logout-url="/resources/j_spring_security_logout" />
        <!-- Configure these elements to secure URIs in your application -->
		<intercept-url pattern="/usuarioempresas/**" access="permitAll" />
		<intercept-url pattern="/demandantes/**" access="permitAll" />
		<intercept-url pattern="/empleadoes/**" access="permitAll" />
		<intercept-url pattern="/puestoes/**" access="hasAnyRole('UsuarioEmpresa','Admin','Empleado')" />
		<intercept-url pattern="/ofertas/**" access="hasAnyRole('UsuarioEmpresa','Admin','Empleado')" />
		<intercept-url pattern="/empresas/**" access="hasAnyRole('UsuarioEmpresa','Admin')" />
		<intercept-url pattern="/sedes/**" access="hasAnyRole('UsuarioEmpresa','Admin')" />
		<intercept-url pattern="/inscripcions/**" access="hasAnyRole('Admin','UsuarioEmpresa','Demandante')" />
		<intercept-url pattern="/perfils/**" access="hasAnyRole('Admin','Demandante')" />
		<intercept-url pattern="/formacions/**" access="hasAnyRole('Admin','Demandante')" />
		<intercept-url pattern="/experiencias/**" access="hasRole('Admin')" />
		<intercept-url pattern="/estadoes/**" access="hasRole('Admin')" />
		<intercept-url pattern="/admins/**" access="hasRole('Admin')" />

        <intercept-url pattern="/choices/**" access="hasRole('Admin')" />
        <intercept-url pattern="/member/**" access="isAuthenticated()" />
        <intercept-url pattern="/resources/**" access="permitAll" />
		<intercept-url pattern="/resources/selenium/**" access="hasRole('Admin')" />
        <intercept-url pattern="/login/**" access="permitAll" />
		<intercept-url pattern="/**" access="permitAll" />

    </http>
    <!-- Configure Authentication mechanism -->
    <authentication-manager alias="authenticationManager">
        <!-- SHA-256 values can be produced using 'echo -n your_desired_password | sha256sum' (using normal *nix environments) -->
        <authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="SELECT apodo AS username,
					clave AS password,
					id AS enabled
					FROM abstract_usuario
					WHERE apodo=?"
				authorities-by-username-query="SELECT apodo AS username,
					dtype AS authority
					FROM abstract_usuario
					WHERE apodo=?" />
        </authentication-provider>
    </authentication-manager>
</beans:beans>
